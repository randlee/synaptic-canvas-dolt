# Sprint 5.4 Completion Report

## Outcome

- BLOCKED: live DoltHub-backed smoke could not progress past package discovery.
- PASS: local-clone proof from Sprint 5.3 remains the only working end-to-end smoke path in this environment.
- PASS: the blocking behavior is now documented with exact remote responses and follow-up items.

## DoltHub-Specific Findings

- The DoltHub repository page for `randlee/synaptic-canvas` is reachable over HTTPS.
- The DoltHub SQL API did not provide a usable catalog for Sprint 5.4.
  - `sc --client http --dolt-database randlee/synaptic-canvas --branch develop --json list` returned `backend_unavailable` with `branch not found`.
  - `sc --client http --dolt-database randlee/synaptic-canvas --branch main --json list` returned `backend_unavailable` with `branch not found`.
  - Direct HTTP probes to `https://www.dolthub.com/api/v1alpha1/randlee/synaptic-canvas/main` produced inconsistent upstream failures, including `query error: branch not found` and upstream connection termination.
- Because `list` and `info` failed at the remote catalog boundary, the remote install / status / validate / upgrade / uninstall / reinstall matrix could not be exercised without bypassing the public CLI contract.

## Local-Clone vs DoltHub Comparison

- Sprint 5.3 local clone:
  - `claude-history` was discoverable.
  - `list`, `info`, `install`, `status`, `validate`, `upgrade`, `uninstall`, and reinstall all passed through `sc --json`.
  - Project and global scopes both validated cleanly.
- Sprint 5.4 DoltHub path:
  - repository page exists, but API-backed catalog reads are not usable.
  - no package discovery means no valid remote install proof exists yet.

## Backend Differences Recorded

- Local `cli` backend can read the seeded catalog and complete end-user flows.
- Remote `http` backend currently fails before package resolution with backend errors that point to branch or service availability problems.
- The failure is environment / publication facing, not install-state facing: the blocker occurs before any local files are written.

## Follow-Up Items

- Verify that the intended live smoke database and branch are actually published and readable through the DoltHub SQL API.
- Confirm whether `randlee/synaptic-canvas` is the correct live database slug for package-catalog smoke testing, or whether Sprint 5.4 should target a different DoltHub-backed database.
- If the branch is correct, investigate why DoltHub HTTP returns `branch not found` for both `main` and `develop` while the repository page remains reachable.
- Add these DoltHub publication and branch-verification checks to future Dolt-oriented skills so live smoke attempts fail fast with targeted guidance.
