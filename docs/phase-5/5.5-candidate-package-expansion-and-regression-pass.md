---
id: 5.5
title: Candidate Package Expansion And Regression Pass
status: planned
branch: feature/p5-s5-package-expansion
target: integrate/phase-5
---

# Sprint 5.5 — Candidate Package Expansion And Regression Pass

## Goal

- Expand the import-and-smoke-test process to the next candidate package set.
- Prove the MVP handles more than one package shape.
- Write down the first shared-infrastructure package-boundary decision before implementation starts.

## Hard Dependencies

- `claude-history` has passed both the local-clone and DoltHub smoke tests.
- The Phase 5 smoke-test procedure is stable enough to rerun across additional packages without redesigning the process.
- The next candidate repositories are available locally:
  - `/Users/randlee/Documents/github/synaptic-canvas/packages/sc-docling-pdf`
  - `/Users/randlee/Documents/github/synaptic-canvas/packages/sc-launchpad`
  - `/Users/randlee/Documents/github/synaptic-canvas/packages/sc-launch-term`
  - `/Users/randlee/Documents/github/atm-core/.claude/skills/sprint-report`

## Normative References

- Requirement: `REQ-003`
- Requirement: `REQ-006`
- Requirement: `CLI-010`
- Requirement: `AG-001`
- Requirement: `VER-003`
- Requirement: `VER-009`
- Requirement: `VER-013`

## Exact Targets

- `/Users/randlee/Documents/github/synaptic-canvas/packages/sc-docling-pdf`
- `/Users/randlee/Documents/github/synaptic-canvas/packages/sc-launchpad`
- `/Users/randlee/Documents/github/synaptic-canvas/packages/sc-launch-term`
- `/Users/randlee/Documents/github/atm-core/.claude/skills/sprint-report`

## Required Work

- Run the documented import-and-smoke-test procedure against the next package set after `claude-history`.
- Keep `sc-docling-pdf` as the curated-runtime case where local install guidance remains substantive.
- Treat `sc-launchpad` and `sc-launch-term` as one package-boundary decision: combine them into a single package with multiple launch skills and shared install/troubleshooting guidance.
- Include `sprint-report` as the template/assets case.
- Review imported skills and agents against `/Users/randlee/Documents/github/synaptic-canvas/docs/claude-code-skills-agents-guidelines.md` before each package is marked ready.
- Record pass/fail evidence and blockers for each package instead of allowing the expansion pass to turn into informal exploratory work.

## Implementation Notes

- Proposed candidate order: `sc-docling-pdf`, combined launch-terminal package, `sprint-report`.
- For the launch-terminal package, keep upstream install guidance external for Claude Code, Codex CLI, and Gemini CLI because those vendor instructions are moving targets.
- For `sc-docling-pdf`, keep local install guidance because the package depends on a tested subset of tools and a curated workflow.
- Save complex inter-package dependency graphs for a later phase unless they block one of these candidate packages directly.

## Non-Goals

- Do not expand into the full marketplace in Sprint 5.5.
- Do not pull in deeper inter-package dependency orchestration yet.
- Do not leave the launch-terminal package split unresolved if the shared-infrastructure case is already clear.

## Acceptance Criteria

- The phase explicitly covers `sc-docling-pdf`, the combined `sc-launchpad`/`sc-launch-term` case, and `sprint-report` after `claude-history`.
- The launch-terminal package merge decision is written down before implementation starts.
- Each candidate package has explicit local-clone and DoltHub smoke-test evidence or a recorded blocker.
- No package is marked complete based on source edits alone without product-level import and smoke evidence.

## Required Validation

- `(cd src && go test ./...)`
- `(cd src && go test ./... -race)`
- `(cd src && golangci-lint run ./...)`
- `git diff --check`

## Completion Report Requirements

- Record package-specific fixes made during Sprint 5.5 for `sc-docling-pdf`, the combined launch-terminal package, and `sprint-report`.
- Record any merge, topology, dependency, or install-doc corrections made while expanding the package set.
- Record any larger cross-package issues that require another sprint as explicit follow-up items rather than burying them in package-specific notes.
